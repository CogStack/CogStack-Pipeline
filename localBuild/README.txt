In this directory are stored scripts to generate locally build custom images used in CogStack ecosystem. 

For the moment, locally build images are only used for fluentd example.

This is a temporary solution, as the build process is being slightly refactored.
